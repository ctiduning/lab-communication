# 增量 PRD：通讯录三级部门架构重构

**文档版本**: v1.0  
**创建日期**: 2025-01-18  
**产品经理**: Alice  
**项目**: QDCTI（青岛华测实验室沟通小程序）  
**文档类型**: 增量 PRD（仅描述变更部分）

---

## 1. 项目信息

- **Language**: 中文
- **Programming Language**: Vue 3 + TypeScript + Vite + Element Plus + Supabase
- **Project Name**: qdcti_department_restructuring
- **原始需求复述**: 
  - 重构现有通讯录结构，从单字段 `department` 改为三级部门架构（一级/二级/三级）
  - 新增"部门名片"功能，允许业务人员选择部门而非个人进行沟通
  - 实现"每部门限选一人"逻辑，避免重复选择同一部门成员

---

## 2. 产品定义

### Product Goals

1. **结构化组织架构**：通过三级部门架构（一级/二级/三级）清晰呈现实验室和业务部门的层级关系，提升通讯录的可读性和可维护性
2. **灵活沟通方式**：引入"部门名片"概念，允许业务人员直接向部门负责人（组长+组长助理）发起沟通，提高沟通效率
3. **防止重复选择**：通过"每部门限选一人"规则，避免在同一沟通中选择同一部门的多个成员，减少信息冗余

### User Stories

1. **As a 业务人员**, I want 在发起沟通时可以选择"部门名片"（如"企业气相组"） so that 我可以直接向该部门的负责人发起沟通，而无需知道具体哪位工程师负责

2. **As a 实验室主管/组长**, I want 当业务选择部门名片后，消息同时发送给组长和组长助理 so that 确保部门负责人都能看到紧急沟通请求，且任一人回复后状态自动同步

3. **As a 业务人员**, I want 每个三级部门最多只能选一个人（或部门名片） so that 避免向同一部门发送重复消息，减少打扰

4. **As a 检测工程师**, I want 我的角色（检测工程师）不参与部门名片功能 so that 业务人员只能单独选择我作为个人联系人，而不能通过部门名片找到我

5. **As a 系统管理员**, I want 通讯录支持三级部门联动选择（一级→二级→三级→角色） so that 用户可以快速定位到目标部门或人员

---

## 3. 技术规范

### Requirements Pool

#### P0（Must Have）

| 需求ID | 需求描述 | 验收标准 |
|--------|----------|----------|
| REQ-001 | 数据库迁移：profiles 表新增 department_level1, department_level2, department_level3 字段 | 1. 执行 SQL 迁移脚本后，profiles 表包含 3 个新字段<br>2. 现有数据自动填充（department 字段映射到 department_level2，region 字段映射到 department_level3）<br>3. 新注册用户必须填写三级部门信息 |
| REQ-002 | 三级部门联动选择逻辑（前端） | 1. 一级部门选择"业务"时，二级部门显示 5 个产品线选项<br>2. 一级部门选择"实验室"时，二级部门显示 3 个实验室选项<br>3. 三级部门根据一级和二级部门动态变化（业务=手动填写属地；实验室=16 个固定组）<br>4. 角色根据一级和二级部门动态变化 |
| REQ-003 | 部门名片功能 | 1. 发起沟通时，通讯录显示"部门名片"选项（仅限有组长/组长助理的部门）<br>2. 选择部门名片后，消息同时发送给该部门的组长+组长助理<br>3. 任一人回复/操作后，另一人的状态同步为"已回复"，无法再操作<br>4. 只取第一个回复的内容作为正式回复 |
| REQ-004 | 每部门限选一人逻辑 | 1. 每个三级部门最多只能选 1 个人（或 1 个部门名片）<br>2. 选了部门名片后，不能再选该部门的个人<br>3. 选了部门个人后，不能再选该部门的部门名片<br>4. 可以跨部门选择（如企业气相组 1 人 + 理化组 1 人） |

#### P1（Should Have）

| 需求ID | 需求描述 | 验收标准 |
|--------|----------|----------|
| REQ-005 | 通讯录界面重构 | 1. 通讯录列表按三级部门架构展示（一级→二级→三级）<br>2. 支持按一级部门、二级部门、三级部门筛选<br>3. 显示部门名片入口（如有）<br>4. 个人名片显示：姓名、工号、三级部门、角色、电话、邮箱 |
| REQ-006 | 消息接收逻辑优化 | 1. 选择部门名片后，消息收件人列表显示"企业气相组（组长+组长助理）"<br>2. 组长和组长助理收到的消息标识为"部门消息"<br>3. 任一人回复后，另一人收到"已回复"通知 |

#### P2（Nice to Have）

| 需求ID | 需求描述 | 验收标准 |
|--------|----------|----------|
| REQ-007 | 部门名片管理后台 | 1. 管理员可以配置哪些部门有"部门名片"<br>2. 管理员可以设置部门名片的接收人（组长/组长助理）<br>3. 支持批量导入/导出部门架构 |
| REQ-008 | 消息状态同步优化 | 1. 组长和组长助理回复消息时，显示"代部门回复"标识<br>2. 业务人员可以看到是哪个负责人回复的 |

---

### UI Design Draft

#### 3.1 通讯录界面（重构后）

**当前界面问题**：
- 通讯录只有平铺列表，无法体现部门层级
- 无法区分"个人名片"和"部门名片"

**重构后界面描述**：

```
┌─────────────────────────────────────┐
│ 通讯录                      [搜索🔍] │
├─────────────────────────────────────┤
│ 筛选：                               │
│ [一级部门 ▼] [二级部门 ▼] [三级部门 ▼]│
├─────────────────────────────────────┤
│ ▼ 业务                               │
│   ├─ 食品产品线                      │
│   │   └─ 青岛                        │
│   │       ├─ 张三 (业务) 📇          │
│   │       └─ 李四 (业务助理) 📇      │
│   └─ 特食及日化产品线                │
│       └─ 青岛                        │
│           └─ 王五 (业务) 📇          │
├─────────────────────────────────────┤
│ ▼ 实验室                             │
│   ├─ 青岛食品企业实验室              │
│   │   ├─ 企业气相组 🏢               │ ← 部门名片
│   │   │   ├─ 赵六 (组长) 📇         │
│   │   │   ├─ 钱七 (组长助理) 📇     │
│   │   │   └─ 孙八 (检测工程师) 📇   │
│   │   └─ 企业液相组 🏢               │ ← 部门名片
│   │       └─ ...                     │
│   └─ 青岛食品实验室                  │
│       └─ 政府气相组 🏢               │
│           └─ ...                     │
└─────────────────────────────────────┘

图例：
  📇 = 个人名片
  🏢 = 部门名片（点击后选择该部门）
```

**关键交互逻辑**：
1. 点击 `🏢 企业气相组` → 选择"部门名片"，消息发给赵六+钱七
2. 点击 `📇 孙八` → 选择"个人名片"，消息只发给孙八
3. 如果已选择"企业气相组（部门名片）"，则孙八、赵六、钱七都置灰不可选
4. 如果已选择"孙八（个人名片）"，则"企业气相组"部门名片置灰不可选

#### 3.2 发起沟通界面（选择收件人）

**当前界面问题**：
- 只能选择个人，无法选择部门
- 可以选择同一部门的多个人

**重构后界面描述**：

```
┌─────────────────────────────────────┐
│ 发起沟通                            │
├─────────────────────────────────────┤
│ 收件人 (已选 2 人)：                 │
│   ✅ 企业气相组 (组长+组长助理) [×] │ ← 部门名片
│   ✅ 孙八 (检测工程师) [×]          │ ← 个人名片（跨部门）
│                                   │
│ 搜索通讯录：____________________    │
│                                   │
│ ▼ 实验室 > 青岛食品企业实验室        │
│   ✅ 企业气相组 🏢 (已选)          │ ← 置灰，不能取消
│   ├─ 赵六 (组长) ❌                │ ← 置灰，因为部门名片已选
│   ├─ 钱七 (组长助理) ❌            │ ← 置灰，因为部门名片已选
│   └─ 孙八 (检测工程师) ❌          │ ← 置灰，因为已选
│                                   │
│ ▼ 实验室 > 青岛食品实验室            │
│   ☐ 政府气相组 🏢                   │ ← 可选
│   └─ ...                           │
└─────────────────────────────────────┘
```

**关键校验逻辑**：
- 选择部门名片后，该部门所有人置灰 ❌
- 选择个人名片后，该部门名片置灰 ❌
- 每个三级部门最多选 1 个（部门名片或个人名片）

#### 3.3 消息接收界面（组长/组长助理）

**场景**：业务人员选择"企业气相组"部门名片，消息发给赵六（组长）和钱七（组长助理）

**赵六的视角**：

```
┌─────────────────────────────────────┐
│ 🏢 部门消息                         │
├─────────────────────────────────────┤
│ 来自：张三 (业务)                   │
│ 部门：企业气相组                     │
│ 时间：2025-01-18 14:30             │
│                                   │
│ 消息内容：                          │
│ "请帮忙检测这批样品，急！"           │
│                                   │
│ [回复] [同意] [拒绝] [转交]         │
│                                   │
│ ℹ️ 此消息同时发送给您和钱七（组长助理）│
│    任一人回复后，另一人将无法操作      │
└─────────────────────────────────────┘
```

**钱七的视角（赵六先回复后）**：

```
┌─────────────────────────────────────┐
│ 🏢 部门消息 [已回复]                │ ← 状态变为"已回复"
├─────────────────────────────────────┤
│ 来自：张三 (业务)                   │
│ 部门：企业气相组                     │
│ 时间：2025-01-18 14:30             │
│ 回复人：赵六                        │
│                                   │
│ 消息内容：                          │
│ "请帮忙检测这批样品，急！"           │
│                                   │
│ 赵六的回复：                        │
│ "好的，明天上午安排。"                │
│                                   │
│ [已回复，无法操作]                   │ ← 按钮置灰
└─────────────────────────────────────┘
```

---

### Open Questions

1. **数据库迁移策略**：
   - 现有 `profiles` 表的 `department` 和 `region` 字段如何映射到新的三级部门字段？
   - 是否需要保留旧字段作为历史数据，还是直接迁移后删除？

2. **部门名片的显示逻辑**：
   - 如果某个部门只有"组长"没有"组长助理"，部门名片是否仍然显示？
   - 如果某个部门既没有"组长"也没有"组长助理"，部门名片是否隐藏？

3. **消息同步的技术实现**：
   - 组长和组长助理同时在线时，如何实时同步"已回复"状态（WebSocket？轮询？）
   - 如果一人回复后，另一人正在输入，如何处理（强制中断？提示已回复？）

4. **角色权限细化**：
   - "实验室主管"和"实验室主管助理"是否也参与部门名片功能？
   - 目前只有"组长"和"组长助理"参与，是否需要扩展到其他角色？

5. **异常处理**：
   - 如果组长和组长助理都被删除/禁用，部门名片如何处理？
   - 如果业务人员选择了部门名片，但组长和组长助理都离职了，消息发给谁？

6. **前端性能优化**：
   - 通讯录列表可能很长（16 个组 × 每组多人），是否需要虚拟滚动？
   - 三级部门联动选择是否需要前端缓存选项数据？

---

## 4. 数据库改动（SQL 迁移脚本示例）

```sql
-- 迁移脚本：profiles 表新增三级部门字段

-- 1. 新增字段
ALTER TABLE profiles 
ADD COLUMN department_level1 VARCHAR(50),  -- 一级部门（业务/实验室）
ADD COLUMN department_level2 VARCHAR(100), -- 二级部门（产品线/实验室名称）
ADD COLUMN department_level3 VARCHAR(100); -- 三级部门（属地/组别）

-- 2. 旧数据迁移（示例）
-- 假设旧数据中：
--   department = '理化检测组' → department_level2 = '青岛食品企业实验室', department_level3 = '理化组'
--   region = '青岛' → 需要根据 department_level2 判断如何填充
UPDATE profiles 
SET 
  department_level1 = CASE 
    WHEN department IN ('企业气相组', '企业液相组', '政府气相组', '政府液相组', '理化组', ...) 
    THEN '实验室' 
    ELSE '业务' 
  END,
  department_level2 = CASE 
    WHEN department IN ('企业气相组', '企业液相组') 
    THEN '青岛食品企业实验室'
    WHEN department IN ('政府气相组', '政府液相组') 
    THEN '青岛食品实验室'
    ELSE department
  END,
  department_level3 = CASE 
    WHEN department IN ('企业气相组', '企业液相组', '政府气相组', '政府液相组', '理化组', ...) 
    THEN department 
    ELSE region
  END
WHERE department_level1 IS NULL;

-- 3. 新增字段非空约束（可选，根据业务需求）
ALTER TABLE profiles 
MODIFY COLUMN department_level1 VARCHAR(50) NOT NULL,
MODIFY COLUMN department_level2 VARCHAR(100) NOT NULL,
MODIFY COLUMN department_level3 VARCHAR(100) NOT NULL;

-- 4. 新增索引（优化查询）
CREATE INDEX idx_profiles_department_level1 ON profiles(department_level1);
CREATE INDEX idx_profiles_department_level2 ON profiles(department_level2);
CREATE INDEX idx_profiles_department_level3 ON profiles(department_level3);
```

**注意**：以上 SQL 仅供参考，实际迁移需要根据现有数据结构详细设计。

---

## 5. 开发建议

### 5.1 前端开发顺序

1. **第一阶段**：数据库迁移 + 三级部门字段新增
2. **第二阶段**：通讯录界面重构（三级部门联动选择）
3. **第三阶段**：部门名片功能（消息发送+接收同步）
4. **第四阶段**：每部门限选一人逻辑
5. **第五阶段**：优化和异常处理

### 5.2 技术风险

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 数据库迁移失败 | 高 | 先在测试环境验证迁移脚本，备份生产数据 |
| 消息同步延迟 | 中 | 使用 WebSocket 实时推送，设置超时重试机制 |
| 前端性能问题 | 中 | 使用虚拟滚动，分页加载通讯录列表 |
| 角色权限混乱 | 高 | 明确角色枚举值，前端+后端双重校验 |

---

## 6. 验收标准（Summary）

✅ **P0 需求必须全部完成并通过测试**  
✅ **部门名片功能核心流程跑通**（选择→发送→双接收→任一人回复→同步）  
✅ **每部门限选一人逻辑生效**（不能重复选择同一部门）  
✅ **现有功能不受影响**（消息撤回/编辑、存储管理、一键备份等）  

---

**文档结束** | 如有疑问，请联系 Product Manager Alice
