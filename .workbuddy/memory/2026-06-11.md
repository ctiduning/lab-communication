# 2026-06-11 工作日志

## 三项需求实现（基于版本1回退后）
1. **公告 🚩 手动标记**：新增 `announcement_flags` 表 API + Announcements.vue 界面，替换关键词自动检测
2. **表格行点击查看详情**：BusinessReceive/LabReceive/SentMessages 全部 el-table 添加 @row-click + .stop
3. **邮件式 Thread 回复**：SentMessages 状态简化为4种，移除接收端完结按钮，保留发起人全局完结

### 变更文件
- src/api/index.js（新增 announcement_flags API）
- src/pages/Announcements.vue（手动红旗标记）
- src/pages/BusinessReceive.vue（row-click + 移除完结按钮）
- src/pages/LabReceive.vue（row-click + 移除完结按钮）
- src/pages/SentMessages.vue（状态简化 + row-click）
- supabase-changes.sql（数据库变更脚本）

### 构建：✅ 通过
### 已推送 GitHub + gh-pages

---

## 21:30 消息未读状态 Bug 修复 (commit `5f66769`)

**问题**：追加回复（follow-up）后，收信方的 `is_read` 没有重置为 `false`，导致无法看到"未读"标识。

**修复**：
1. `src/api/index.js` — `createReply` follow-up 分支在 `communication_recipients.update()` 中补充 `is_read: false`
2. `src/pages/SentMessages.vue` — `submitFollowUp` 中同样补充 `is_read: false`

**分支**: `perf/optimization`
