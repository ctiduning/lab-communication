# 2026-06-11 工作日志

## 已发送页面（SentMessages.vue）红旗功能
- 主表格操作列宽度 160→260，已撤回表格 200→290，末尾加红旗按钮
- 详情弹窗 footer 加红旗按钮
- 使用 `toggleCommFlag`（communications 表级），非 `toggleRecipientFlag`
- 修复：工程师误用 `toggleRecipientFlag` 传了发送人 ID（查不到记录），改为 `toggleCommFlag`

## 公告青岛/非青岛业务筛选
- 发送范围从 3 个增加到 5 个：全部用户/全部业务/全部实验室/青岛业务/非青岛业务
- api/index.js create 增加 target_regions 参数
- 过滤逻辑：department_level1 === '业务' + department_level3 判断青岛

## 回复记录 inline 追加回复
- 每条 reply 右侧 💬 按钮，点击展开 inline 输入框
- BusinessReceive.vue 和 SentMessages.vue 双端实现
- 发送成功后重新加载详情

## 2026-06-15 补充
- 三个功能完成 GitHub 推送（commit `4d0ca7f`）
- Code Review 发现并修复 `createReply` 中 `.single()` 改为 `.maybeSingle()`（发送人不在 recipients 表时抛异常）
- 修复 Admin.vue 中 `r.profiles?.name` → `r.recipient?.name`
- 输出 CHANGELOG-2026-06-15.md 更新日志文件
