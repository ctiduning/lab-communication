# 已撤回消息展示逻辑修改 - 完成总结

## 修改内容

### 1. SentMessages.vue（发件人端）
- **主表格**：已撤回消息现在所有标签页（全部/已发送/待回复/已回复/已完成/已完结）均展示
- **详情弹窗**：撤回按钮现在始终可用（取消 5 分钟时间限制）
- **已撤回标签页**：操作列宽度调整（80→150），添加红旗按钮
- **状态列**：添加已撤回状态显示（`isRecalled && !hasReplied` → 已撤回）
- **行样式**：已撤回行淡红色背景
- **`canRecall` 函数**：移除 5 分钟时间窗口限制

### 2. BusinessReceive.vue（商务端收件人）
- **计算属性**：全部/已处理/已完结/红旗标签移除 `!m.isRecalled` 过滤
- **待处理标签**：保持不变，仍过滤已撤回消息
- **状态列**：添加已撤回判断逻辑（已完结 > 已撤回(未回复) > 已回复 > 待处理）
- **已被撤回标签页操作列**：宽度 80→150，添加红旗按钮
- **行样式 + CSS**：`.recalled-row` 淡红色背景

### 3. LabReceive.vue（实验室端收件人）
- 与商务端同步修改，逻辑完全一致

### 4. api/index.js（后端）
- `recallMessage` 接口本身没有时间限制代码（注释与实际不符），故无需修改

## 关键决策
- 已撤回消息在所有标签页展示（待处理标签除外），让消息可见但标记为已撤回状态
- 撤回时间限制完全移除，用户可以随时撤回自己发送的消息
- 行样式用淡红色背景区分已撤回消息，视觉上明显但不刺眼
